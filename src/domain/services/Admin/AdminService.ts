/**
 * Service ini berfungsi re-check lagi sebelum kirim ke repository
 * perlu domain/model juga agar balikan functionya itu tidak any
 * ex: validasi menggunakan joi
 */
// export async function UpdateOrderStatus(params: UserParamsDto.UpdateOrderStatus) {
//     return await UserRepository.DBUpdateOrderStatus(params)
// }
